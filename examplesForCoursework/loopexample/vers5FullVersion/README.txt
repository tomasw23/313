This version extends vers4VerificationConditionsNoOverflowCheck by 
extending the verification conditions so that it passes the range checks.
It succeeds SPARK Ada prove all 
     if started from  main.gpr

It can be compiled by selecting the main.adb file and choosing
Build -> Project -> Build <current file>
and then  run
using 
Build -> Run -> Custom   
    selecting external terminal
and executing in that terminal
    main  
